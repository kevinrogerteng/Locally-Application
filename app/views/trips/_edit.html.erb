	<%= form_for (@trip) do |f| %>
		<%= label_tag(:name, "Trip Name") %> <%= f.text_field :name%><br>
		<%= label_tag(:destination, "Destination") %> <%= f.text_field :destination %><br>
		<%= label_tag(:description, "Trip Description") %> <%= f.text_field :description %><br>
	
		<%= f.submit "Update Trip"%>
	<%end%>
